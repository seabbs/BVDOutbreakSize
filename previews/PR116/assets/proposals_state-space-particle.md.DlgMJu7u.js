import{c as t,S as a,j as s,m as i}from"./chunks/framework.D9enq00w.js";const u=JSON.parse('{"title":"Proposal: state-space stochastic infection process with particle-filter inference","description":"","frontmatter":{},"headers":[],"relativePath":"proposals/state-space-particle.md","filePath":"proposals/state-space-particle.md","lastUpdated":null}'),o={name:"proposals/state-space-particle.md"};function n(r,e,l,c,d,h){return a(),s("div",null,[...e[0]||(e[0]=[i(`<h1 id="Proposal:-state-space-stochastic-infection-process-with-particle-filter-inference" tabindex="-1">Proposal: state-space stochastic infection process with particle-filter inference <a class="header-anchor" href="#Proposal:-state-space-stochastic-infection-process-with-particle-filter-inference" aria-label="Permalink to &quot;Proposal: state-space stochastic infection process with particle-filter inference {#Proposal:-state-space-stochastic-infection-process-with-particle-filter-inference}&quot;">​</a></h1><p>Status: candidate redesign on the <code>arch-state-space</code> branch. Additive to the existing model; the deterministic exponential-growth baseline is unaffected.</p><h2 id="Motivation-and-contrast-with-the-sibling-tracks" tabindex="-1">Motivation and contrast with the sibling tracks <a class="header-anchor" href="#Motivation-and-contrast-with-the-sibling-tracks" aria-label="Permalink to &quot;Motivation and contrast with the sibling tracks {#Motivation-and-contrast-with-the-sibling-tracks}&quot;">​</a></h2><p>The baseline model represents the latent infection process as a deterministic continuous-time curve <code>C(s) = exp(r·s)</code>. Every observation likelihood is built from that one smooth trajectory. At the start of the outbreak the population of infected people is O(1-10) and the real trajectory is dominated by demographic stochasticity: who infects whom, and exactly when each early case occurs. A deterministic curve has zero variance there.</p><p>Two sibling tracks already address this gap.</p><ul><li>The renewal architecture (<code>arch-renewal</code>, PR #103) replaces <code>exp(r·s)</code> with a deterministic discrete-time renewal <code>I_t = R_t · sum(I_{t-s} · g_s)</code> driven by a weekly random-walk <code>R_t</code>.</li></ul><p>The latent infection process is still deterministic given <code>R_t</code>; only <code>R_t</code> carries the noise.</p><ul><li>The stochastic-latent track (<code>arch-stochastic</code>, PR #101) adds Gaussian process noise to <code>log C(s)</code>, i.e. a log-Gaussian / linear-noise approximation (LNA) of the underlying stochastic infection process.</li></ul><p>This keeps the early-phase variance but represents it as a continuous Gaussian relaxation, so the whole model stays differentiable and NUTS + Mooncake samples it directly.</p><p>This proposal takes the next step honestly. The exact discrete-time stochastic infection process is a branching process with overdispersed offspring: each infected case generates a random integer number of secondaries drawn from a negative binomial, and the secondaries&#39; infection times are drawn from the generation-interval PMF. That process has <em>discrete</em> latent counts, so NUTS cannot differentiate through it. The Gaussian relaxation in <code>arch-stochastic</code> is one way out; the other is to write the model as an explicit state-space model and infer the latent trajectory by particle methods (particle filtering / particle Gibbs). This proposal is that route.</p><p>Comparison axis the owner asked for: when does the exact discrete latent pay off in inference about the early phase, versus the Gaussian relaxation? The answer is honest and largely negative for this outbreak; see the verdict.</p><h2 id="Generative-model" tabindex="-1">Generative model <a class="header-anchor" href="#Generative-model" aria-label="Permalink to &quot;Generative model {#Generative-model}&quot;">​</a></h2><h3 id="Latent-state" tabindex="-1">Latent state <a class="header-anchor" href="#Latent-state" aria-label="Permalink to &quot;Latent state {#Latent-state}&quot;">​</a></h3><p>A daily grid <code>t = 1, …, n</code>, where <code>n</code> is the data cut-off (so day <code>n</code> is the as-of date). Day 1 represents the seeding event, with a small seed <code>I_1</code>. Conditional on the per-day reproduction number <code>R_t</code> (continuous, sampled by a weekly random walk) and a fixed offspring overdispersion <code>φ</code>, the <em>force of infection</em> on day <code>t</code> is</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>λ_t = R_t · sum_{s ≥ 1} I_{t-s} · g_s</span></span></code></pre></div><p>with <code>g</code> the generation-interval PMF. The latent infection count on day <code>t</code> is</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>I_t | I_{&lt;t}, R_t  ~  NegativeBinomial(mean = λ_t, dispersion = φ),</span></span></code></pre></div><p>with <code>Var(I_t) = λ_t + λ_t² / φ</code>. This is the exact discrete branching-process step at the population level: the negative binomial is the marginal for sum of NB-offspring across infectors when each infector&#39;s offspring distribution is NB. It collapses to a Poisson branching process as <code>φ → ∞</code>. A Poisson alternative (<code>φ = ∞</code>) is left selectable by prior on <code>φ</code> (a wide half-Normal with substantial mass at large values), so the Poisson nesting is recovered.</p><p><code>I_1</code> is the <em>seed size</em> with a small-integer prior, allowing for a small primary cluster rather than a hard single zoonotic case. The default prior is <code>I_1 ~ 1 + Poisson(0.5)</code> (median 1, occasional 2-3), so the single-seed baseline is the prior mode.</p><h3 id="Observation-staging-—-same-as-the-convolution-/-renewal-route" tabindex="-1">Observation staging — same as the convolution / renewal route <a class="header-anchor" href="#Observation-staging-—-same-as-the-convolution-/-renewal-route" aria-label="Permalink to &quot;Observation staging — same as the convolution / renewal route {#Observation-staging-—-same-as-the-convolution-/-renewal-route}&quot;">​</a></h3><p>The observation submodels never touch the daily infections directly. Infections are first convolved with the incubation PMF to give the onset incidence <code>O_t</code>, and onsets are convolved with the relevant onset-to-X delay PMF to give the daily count of the observed event timed at observation:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>O_t                 = sum_{d ≥ 0} I_{t-d}    · h_incub[d]</span></span>
<span class="line"><span>deaths_t            = CFR · sum_{d ≥ 0} O_{t-d} · h_death[d]</span></span>
<span class="line"><span>reports_t           = p_DRC · sum_{d ≥ 0} O_{t-d} · h_report[d]</span></span>
<span class="line"><span>exports_t           = p_Uganda · q · sum_{d ≥ 0} O_{t-d} · h_detect[d]</span></span>
<span class="line"><span>export_deaths_t     = CFR · p_Uganda · q · sum_{d ≥ 0} O_{t-d} · h_death[d]</span></span></code></pre></div><p>This is the same onset-staging the discrete-renewal architecture uses; it makes detection and death consistently timed from onset.</p><h3 id="Observation-likelihoods" tabindex="-1">Observation likelihoods <a class="header-anchor" href="#Observation-likelihoods" aria-label="Permalink to &quot;Observation likelihoods {#Observation-likelihoods}&quot;">​</a></h3><p>Observations condition on the available <em>cumulative</em> totals through to the data cut-off, exactly as the baseline model does:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>exported_cases  ~ Poisson(sum(exports_t))</span></span>
<span class="line"><span>total_deaths    ~ NegBinomial(sum(deaths_t), k)</span></span>
<span class="line"><span>reported_cases  ~ NegBinomial(sum(reports_t), k)</span></span>
<span class="line"><span>exports_deaths  ~ Poisson(sum(export_deaths_t))</span></span></code></pre></div><p>Where time-resolved data are available — for example the dated export deaths the package already loads, or the sitrep vintage trajectory of PR #107 — the same <code>deaths_t</code> / <code>export_deaths_t</code> series plugs in as the intensity of a binned-Poisson likelihood (matching the existing <code>exports_deaths_model</code>). The cumulative version is demonstrated end-to-end here; the daily version is a drop-in replacement on the same vectors.</p><h3 id="Genetic-TMRCA" tabindex="-1">Genetic TMRCA <a class="header-anchor" href="#Genetic-TMRCA" aria-label="Permalink to &quot;Genetic TMRCA {#Genetic-TMRCA}&quot;">​</a></h3><p>The soft lower bound on the outbreak age <code>T = n</code> is added as a censored upper-tail term, identical to the baseline <code>genetic_seeding_model</code>:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>log p_gen(T) = log Φ((n - tmrca_days) / tmrca_days_sd).</span></span></code></pre></div><p>Because <code>n</code> is fixed here (set by the daily-grid length), the bound is effectively a one-sided likelihood on <code>n</code>. A version that samples <code>n</code> (the outbreak age) is possible but materially complicates the particle filter (the latent grid length itself becomes a random dimension).</p><h2 id="Design-rules" tabindex="-1">Design rules <a class="header-anchor" href="#Design-rules" aria-label="Permalink to &quot;Design rules {#Design-rules}&quot;">​</a></h2><p>Mirroring the main model and the renewal architecture:</p><ul><li>Every prior is a submodel, passed in.</li></ul><p>The composer accepts <code>gi</code>, <code>incubation</code>, <code>onset_to_death</code>, <code>onset_to_report</code>, <code>onset_to_detection</code>, <code>cfr</code>, <code>dispersion</code>, <code>ascertainment</code>, <code>traveller</code>, <code>window</code>, <code>genetic</code>, <code>seed</code>, <code>rt</code>, and <code>offspring</code> building-block submodels as keyword arguments with sensible defaults.</p><ul><li>All delays carry uncertainty.</li></ul><p>Generation interval, incubation, onset-to-death, onset-to-report and onset-to-detection are each sampled with priors on their distribution parameters, so no fixed delay constants are buried in the model body. The generation interval defaults to a Gamma with <code>α, θ</code> priors mirroring the onset-to-death delay shape.</p><ul><li>Observation distributions are injected.</li></ul><p>The Poisson and NegBinomial likelihoods come from a small per-stream observation-error submodel where the dispersion / link is parameterised; this keeps the dispersion <code>k</code> swappable.</p><ul><li>No inline <code>using</code> / <code>import</code> in <code>src/state_space.jl</code>.</li></ul><p>All imports stay on the module page; the file relies on names already imported by <code>BVDOutbreakSize.jl</code>.</p><ul><li><p>Onset staging once, reused: <code>O_t</code> is computed once and convolved separately for each downstream stream.</p></li><li><p>Non-centred parameterisation for the weekly random-walk knots and the pooled ascertainment offsets, matching the baseline pattern.</p></li><li><p>Delay discretisation by hand-rolled trapezoidal integration over daily bins with the density at zero forced to zero, the same AD-safe trick the renewal architecture uses (CensoredDistributions&#39; analytical primary-censored CDF is not Mooncake-differentiable; see the renewal track&#39;s verdict).</p></li></ul><p>This is a single-interval bin rather than a full double-interval-censored PMF; the bias from the missing second censoring is small (a few percent on the central mass) and is documented honestly here rather than chased into AD-unfriendly machinery.</p><h2 id="Inference-plan" tabindex="-1">Inference plan <a class="header-anchor" href="#Inference-plan" aria-label="Permalink to &quot;Inference plan {#Inference-plan}&quot;">​</a></h2><p>The latent state is the daily infection trajectory <code>I_{1:n}</code>, a vector of non-negative integers. NUTS cannot sample it. The continuous nuisance parameters (the weekly log-<code>R_t</code> knots, the random-walk SD, <code>CFR</code>, the delay shape/scale parameters, the ascertainment hyperparameters and offsets, the dispersion, the traveller volume, the detection window, the seed size&#39;s continuous embedding when used) are differentiable.</p><p>The intended sampler is therefore a Turing <code>Gibbs(:I =&gt; PG(N), :nuisances =&gt; NUTS(adtype))</code> block:</p><ul><li><p><code>PG(N)</code> (particle Gibbs) updates the latent integer trajectory by running a conditional SMC sweep with <code>N</code> particles, conditional on the current nuisance values.</p></li><li><p><code>NUTS</code> updates the continuous nuisances by Hamiltonian Monte Carlo with Mooncake gradients, conditional on the latent trajectory.</p></li></ul><p>In Turing 0.45 the cleanest way to expose the latent counts to PG is to declare them inside the <code>@model</code> as</p><div class="language-julia vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">julia</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">for</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> t </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">in</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 2</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">n</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    I[t] </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">~</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> NegativeBinomial</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(mean </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> λ_t, dispersion </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> φ)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">end</span></span></code></pre></div><p>i.e. via the <code>~</code> distribution, so PG can step them. PG then resamples ancestor particles at each <code>~</code> step.</p><h3 id="Realistic-feasibility-the-verdict" tabindex="-1">Realistic feasibility (the verdict) <a class="header-anchor" href="#Realistic-feasibility-the-verdict" aria-label="Permalink to &quot;Realistic feasibility (the verdict) {#Realistic-feasibility-the-verdict}&quot;">​</a></h3><p>The implementation here covers the full generative model and demonstrates:</p><ul><li><p>It compiles end-to-end as a Turing <code>@model</code>.</p></li><li><p>A prior-predictive draw gives a finite, integer-valued <code>I_{1:n}</code> trajectory and finite expected counts for every stream.</p></li><li><p>A short particle-Gibbs smoke test runs to completion (a handful of Gibbs sweeps with a small particle count) and records finite log-likelihoods, demonstrating the inference route is mechanically operational.</p></li><li><p>A full posterior fit is not run here; the per-iteration cost dominates.</p></li></ul><p>The cost calculation is honest. PG&#39;s per-Gibbs-sweep cost scales as <code>O(N · n)</code> for a <code>n</code>-day grid with <code>N</code> particles, and each &quot;particle step&quot; is a likelihood evaluation across all streams&#39; daily contributions. For <code>n ≈ 130 days</code> and <code>N = 200</code> particles (a defensible minimum for a moderately overdispersed daily process before the particle population collapses), one Gibbs sweep is roughly <code>n · N ≈ 26000</code> forward-model evaluations of the daily kernel, against a single forward-model evaluation per NUTS leapfrog in the LNA track. A NUTS step typically does 10-200 leapfrog evaluations, so one PG-NUTS sweep costs roughly <code>10-100×</code> a single NUTS step in the LNA track. Translated to wall clock, expect:</p><ul><li><p>LNA track full fit: ~15-25 min (per its own measurements).</p></li><li><p>PG + NUTS full fit on the same problem: ~5-25 h for 1000 post-warmup samples per chain, depending on particle count and acceptance rates.</p></li></ul><p>This is a back-of-envelope figure, not a measurement, and is honest about being a rough order of magnitude.</p><p>Particle Gibbs is also known to mix poorly when the latent path is long and the observation noise is small relative to the process noise — exactly the regime here where four aggregate counts pin a 130-day daily trajectory with high latent overdispersion. Practically this means the path-mixing rate of PG (the fraction of latent days that change between consecutive sweeps) tends to be low at the beginning of the path, where there is the least observational anchor. That is precisely the early phase the architecture is meant to learn about, so the asymptotic-mixing argument cuts the wrong way.</p><h3 id="Why-not-pure-SMC-/-particle-marginal-Metropolis-Hastings?" tabindex="-1">Why not pure SMC / particle marginal Metropolis-Hastings? <a class="header-anchor" href="#Why-not-pure-SMC-/-particle-marginal-Metropolis-Hastings?" aria-label="Permalink to &quot;Why not pure SMC / particle marginal Metropolis-Hastings? {#Why-not-pure-SMC-/-particle-marginal-Metropolis-Hastings?}&quot;">​</a></h3><p>A particle marginal MH would marginalise the latent path and step the nuisances on the SMC marginal likelihood. This is a viable alternative but trades latent-path mixing for nuisance-step acceptance, and the same per-sweep cost applies. We do not implement it here because the diagnostic and tuning machinery is less mature in Turing.</p><h2 id="Comparison-to-the-LNA-track-arch-stochastic,-#101" tabindex="-1">Comparison to the LNA track (<code>arch-stochastic</code>, #101) <a class="header-anchor" href="#Comparison-to-the-LNA-track-arch-stochastic,-#101" aria-label="Permalink to &quot;Comparison to the LNA track (\`arch-stochastic\`, #101) {#Comparison-to-the-LNA-track-arch-stochastic,-#101}&quot;">​</a></h2><p>What the exact discrete process buys, in principle:</p><ul><li>Early-phase non-Gaussianity.</li></ul><p>Branching processes have heavy tails: in any one realisation a small early cluster of three to five secondaries can occur and persist as a fluctuation above the mean. The Gaussian LNA squashes that into a log-normal tail, which slightly under-represents the chance of an early cluster. For inference about the <em>minimum</em> outbreak age that is consistent with the data, the exact process gives marginally more posterior mass to &quot;old outbreak with a slow early phase that happened to fluctuate up early&quot;.</p><ul><li>Integer realism at small counts.</li></ul><p>The LNA&#39;s continuous trajectory passes through fractional infection counts (e.g. <code>I_3 = 1.7</code>) that have no integer interpretation, while the state-space model produces integer trajectories the whole way. For observation likelihoods that condition on aggregate counts this is irrelevant; for inferences that read off the exact day-of-first-case it is not.</p><p>What it does not buy, for this outbreak:</p><ul><li>Cumulative-count likelihoods.</li></ul><p>All four primary streams are aggregate cumulatives. Sums smooth over the discrete jumps of the latent path almost completely. By the central limit theorem the cumulative is approximately Gaussian regardless of whether the daily steps are exact NB or LNA, so the Gaussian relaxation gives essentially the same posterior on <code>C(T)</code>.</p><ul><li>The earliest-onset bound.</li></ul><p>The LNA track shows that the censored upper-tail construction is what makes the earliest onset usable. That bound only depends on <code>T</code>, not on the latent path&#39;s discreteness, so the exact process gives no additional handle on it.</p><ul><li>The genetic TMRCA bound.</li></ul><p>Same as the earliest-onset bound: it constrains <code>T</code>, not the path&#39;s discreteness.</p><p>The honest read-out: the LNA track gets most of what the exact discrete process gives, at much lower cost, because all the available observations are aggregates. A time-resolved daily stream that resolves the early phase (e.g. dated infection-onset records day-by-day from the first onset) would tilt the comparison toward the exact process, because then the daily-count tail-behaviour starts to matter. The current data does not have such a stream.</p><h2 id="Identifiability-risks" tabindex="-1">Identifiability risks <a class="header-anchor" href="#Identifiability-risks" aria-label="Permalink to &quot;Identifiability risks {#Identifiability-risks}&quot;">​</a></h2><ul><li>Offspring overdispersion <code>φ</code> versus process noise versus surveillance dispersion <code>k</code>.</li></ul><p>With aggregate counts only, <code>φ</code>, <code>k</code>, and any random-walk noise on <code>R_t</code> all add to the spread of the observed totals. The prior on <code>φ</code> will carry the inference; the streams cannot separate it from <code>k</code>.</p><ul><li>Seed size <code>I_1</code>.</li></ul><p>The seed is poorly identified. A larger seed <code>I_1</code> and a smaller growth rate <code>R_1</code> are nearly degenerate in their effect on <code>C(T)</code>, just as <code>T</code> and <code>r</code> are in the baseline.</p><ul><li>Particle population collapse.</li></ul><p>For long latent trajectories with mild observation noise, the ancestry collapses and effective sample size drops at the start of the path. This is a well-known pathology; remedies (stratified resampling, ancestor sampling) are partial.</p><ul><li>Drift from the deterministic baseline.</li></ul><p>At <code>R_t = exp(r)</code> constant and <code>φ = ∞</code> and <code>I_1 = 1</code>, the discrete branching process has expectation <code>E[I_t] = R^{t-1}</code> and <code>E[C_t] = (R^t - 1)/(R - 1)</code>, which differs from the continuous <code>exp(r·s)</code> by a small constant factor near the seed. This is intentional and is a feature of moving to discrete time, not a bug.</p><h2 id="Files" tabindex="-1">Files <a class="header-anchor" href="#Files" aria-label="Permalink to &quot;Files {#Files}&quot;">​</a></h2><ul><li><p><code>src/state_space.jl</code> — package code: building-block submodels, the discrete NB-branching renewal step, the onset-staging convolutions, the <code>state_space_joint</code> composer.</p></li><li><p><code>test/test_state_space.jl</code> — unit tests for the renewal step, the delay discretisation, the convolution helper, the rt walk, the composer&#39;s prior-predictive, and an end-to-end short PG smoke test.</p></li><li><p><code>scripts/state_space.jl</code> — driver: prior-predictive sample, gradient check on the continuous block, short particle-Gibbs run, console summary.</p></li><li><p><code>docs/src/proposals/state-space-particle.md</code> — this proposal.</p></li></ul><h2 id="Verdict-and-recommendation" tabindex="-1">Verdict and recommendation <a class="header-anchor" href="#Verdict-and-recommendation" aria-label="Permalink to &quot;Verdict and recommendation {#Verdict-and-recommendation}&quot;">​</a></h2><p>The architecture is mechanically operational: it compiles, draws from the prior, and a short particle-Gibbs run completes. The cost is roughly an order of magnitude or more above the LNA track, and the inferential pay-off on the available data is small because all the observations are aggregates that the LNA already captures faithfully.</p><p>Recommendation: do not pursue this as the default architecture for the current data. The LNA track (<code>arch-stochastic</code>, #101) is the right route for production while the streams remain aggregate. The state-space architecture here is worth keeping as a reference point: if a future data drop adds genuinely time-resolved early-phase records (a day-by-day first-onset trail, or sequenced cases with sampling dates that anchor the early branching), the discreteness of the latent path starts to matter, and the implementation here is the seed of the right route at that point.</p>`,87)])])}const m=t(o,[["render",n]]);export{u as __pageData,m as default};
